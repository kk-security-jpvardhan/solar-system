   name: CI
   on: [push]
   jobs:
     call-ci:
       uses: kk-security-jpvardhan/.github/.github/workflows/reusable-ci.yml@main
       with:
         node-version: '20'
       secrets:
         NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
     test-composite-action:
       runs-on: [self-hosted, ubuntu-private]
       steps:
         - uses: kk-security-jpvardhan/lab-actions/setup-env@main
           with:
             environment: 'staging'
     deploy:
       runs-on: ubuntu-latest
       environment: production-env
       steps:
         - run: echo "Deploying with ${{ secrets.PROD_DB_PASSWORD }}"
